const chalk = require('chalk')

module.exports = async (mcb) => {
    /*mcb.on('chat', (username, message) => {
        if(message.startsWith('>')) {
            console.log(chalk`<${username}> {greenBright ${message}}`)
        }
        else {
            console.log(`<${username}> ${message}`)
        }
    })*/
}